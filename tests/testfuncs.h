FunctionValue func(const vec & x, const int evaluateGradient);
FunctionValue rosenbrockBanana(const vec & x, const int evaluateGradient);
FunctionValue rosen(const vec & x, const int evaluateGradient, const int n=2);
